class Solution {
    public boolean check(int[] N) {
        int count = 0;
        int n = N.length;
        for( int i = 0 ; i < n ; i++ ){
            if( N[i] > N[(i+1)%n] ){
                count++;
            }
        }
        return count <= 1;
    }
}
